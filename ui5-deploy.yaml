# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: zfc.wm.zwmpicking
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://fcdestd51.de.faber-castell.local:8010
          client: "010"
        app:
          name: ZUI_WM_PICKING
          package: ZBR_EY_UI5_WM004
          transport: D51K921236
        exclude:
          - /test/
